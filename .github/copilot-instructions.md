# Copilot Instructions — NestWeaver

> Auto-generated by NestWeaver. Provides codebase intelligence via MCP.

## Available MCP Tools (38)

    ### Core retrieval
    - **brain_context** — Use FIRST when you need codebase or knowledge-base context around a symbol, note
    - **brain_search** — Use when you need to find specific notes, headings, sections, tags, or code symb
    - **note_get** — Use after brain_context or brain_search indicates a specific note is relevant an
    - **backlinks** — Use to find every note that wiki-links TO a specific target note. Returns each s
    - **project_context** — Use when you need the full context for a specific named project. Returns all Not

    ### Analysis
    - **cross_repo_contracts** — Use when modifying a symbol that may be shared across multiple repositories to u
    - **brain_impact** — Use BEFORE modifying a function, class, or interface to understand what might br
    - **flow_trace** — Use when you need to understand execution flow: what functions a symbol calls, w
    - **detect_changes** — Use BEFORE committing or reviewing changes to understand their blast radius at t
    - **clusters** — Use to understand the high-level architecture of the codebase by viewing functio
    - **dead_code** — Use when you want to find cleanup opportunities or understand code coverage gaps
    - **hub_nodes** — Use when you need to identify the most connected symbols in the codebase — the c
    - **bridge_nodes** — Use when you need to find architectural chokepoints — symbols that sit on many s
    - **blast_radius** — Use BEFORE merging a PR or after staging changes to understand the full blast ra
    - **affected_tests** — Use to prioritize which test files an MR/PR should run for a set of code changes
    - **contract_drift** — Use to audit API contract drift in the indexed code graph: routes/methods/operat

    ### Investigation
    - **investigate** — Use to orient yourself on an unfamiliar topic, feature, or subsystem in ONE call
    - **investigate_expand** — Use after `investigate` to drill into specific map entries. Given a `bundle_id` 
    - **investigate_hydrate** — Use after `investigate` to fill in source bodies/summaries for every map entry t

    ### Code search
    - **read_symbols** — Use when you need to READ a symbol's source — return just that symbol's span (st
    - **regex_search** — Use when you need to find text by REGEX across indexed content (markdown section
    - **count_patterns** — Use when you only need COUNTS of regex matches across indexed text, not the matc

    ### Status & maintenance
    - **brain_status** — Use at the start of a session to see what knowledge sources are indexed and avai
    - **brain_add_source** — Use when the user mentions notes, vaults, or repos that are not yet indexed, or 
    - **brain_guide** — Use at the very start of a session to get a comprehensive overview of the indexe
    - **stale_check** — Use at the start of a session or after the user makes code changes to verify the
    - **brain_diff** — Use before a code review or after pulling new changes to see what changed since 
    - **get_summary** — Use when you need a compact architectural overview without reading raw code file

    ### Extensions
    - **set_extension** — Use to attach custom metadata to any node (symbol, note, section, tag) in the br
    - **query_extensions** — Use to find nodes by custom metadata or to inspect all properties on a specific 

    ### Vault health
    - **brain_broken_links** — Use when auditing a markdown vault for wikilinks that did not resolve cleanly — 
    - **brain_orphan_documents** — Use to find notes that are disconnected from the knowledge graph — notes with ZE
    - **brain_topic_clusters** — Use to discover the thematic structure of a markdown vault: runs Leiden communit
    - **brain_tag_graph** — Use to understand how tags relate to each other in a markdown vault. Two modes. 
    - **brain_doc_stats** — Use for a one-shot health summary of a markdown vault's document graph. Composes

    ### Memory
    - **brain_memory_lint** — Use to audit a markdown 'memory bank' vault for health problems. Runs SEVEN chec
    - **brain_memory_consolidate** — Use to propose promotions of vault notes UP the memory tiers (daily logs → ideas
    - **brain_memory_related** — Use to walk the TYPED relationship graph from a note — Supersedes / DependsOn / 

    ## When to Use

- Starting a task: call `brain_context` with task keywords
- Before modifying code: call `brain_impact` on the function
- Exploring unfamiliar code: call `brain_search`
- Finding dead code: call `dead_code` for unreachable symbols
- Architecture overview: call `hub_nodes`, `clusters`, and `get_summary`

## Interaction Memory

When the MCP server is started with `--track-interactions`, NestWeaver learns from agent query patterns to improve retrieval ranking over time. Opt-in, local-only, records UIDs and timestamps only — no content is captured. Use `interactions status` to view memory stats and `interactions clear` to wipe interaction data.
