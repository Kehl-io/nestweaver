// Phase 1: Node/edge reducers removed during Sigma -> R3F migration.
// Filtering and hover effects will be reimplemented via instance attribute
// updates in Phase 2.

export function GraphReducers() {
  return null;
}
