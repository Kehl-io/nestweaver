extends:
  - "@commitlint/config-conventional"

rules:
  scope-enum:
    - 2
    - always
    - - schema
      - parser
      - resolver
      - store
      - storage
      - engine
      - mcp
      - ci
      - deps
      - docs
      - release
